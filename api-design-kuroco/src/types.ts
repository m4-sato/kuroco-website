// src/types.ts
export type Case = {
  topics_id: number;
  subject: string;
  slug: string; // ステータス判定に使う
};

export type Tool = {
  topics_id: number;
  subject: string;
  slug: string; // ステータス判定に使う
};
